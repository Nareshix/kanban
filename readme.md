iced and egui branch. main branch is egui got seperate iced

make it adaptive TODO


```rust
use egui_shadcn::{
    avatar, badge, button, calendar_with_props, card, date_picker_with_props, dialog, separator,
    table, table_body, table_cell, table_head, table_header, table_row, tabs, AvatarProps,
    AvatarSize, BadgeProps, BadgeVariant, CalendarMode, CalendarProps, CardProps, CardVariant,
    ControlSize, ControlVariant, DatePickerProps, DialogAlign, DialogProps, Input, InputSize,
    Label, SeparatorProps, TabItem, TableCellProps, TableProps, TableRowProps, TabsProps,
    TabsVariant, Theme,
};
```